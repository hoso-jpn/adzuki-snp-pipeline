manifest {
    name            = 'hoso-jpn/adzuki-snp-pipeline'
    author          = 'Yusuke Hosokawa'
    homePage        = 'https://github.com/hoso-jpn/adzuki-snp-pipeline'
    description     = 'A reproducible SNP-calling pipeline for adzuki bean'
    mainScript      = 'main.nf'
    nextflowVersion = '>=26.04.0'
    version         = '0.1.0-dev'
}

plugins {
    id 'nf-schema@2.8.0'
}

process.executor = 'local'

validation {
    help {
        enabled = true
    }
    logging {
        unrecognisedParams  = 'error'
        unrecognisedHeaders = 'error'
    }
}

profiles {
    test {
        includeConfig 'conf/test.config'
    }
}
