// nf-test-specific overrides. The synthetic fixture parameters and
// container resource limits already come from the pipeline's own
// `test` profile (conf/test.config) and `docker`/`docker_amd64`
// profiles (nextflow.config), selected via nf-test.config's
// `profile` setting.
